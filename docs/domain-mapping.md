# Domain mapping

- THC app (`apps/thc-web`) -> `chunks-cc-faceoff.web.app`
- CC app (`apps/cc-web`) -> `chunks-tron-thc.web.app`
- Shared API -> `firebase/ai-functions`

The naming feels semantically swapped, but this workspace preserves the mapping exactly as requested.
